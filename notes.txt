what users can style :
- order of the sections in the resume



themeable resume — ideas to expand (general options only; avoid turning every token into a user knob):
- type scale / font pairing (in progress — presets, not per-field sizes)
- margin & line-spacing presets (in progress)
- one accent treatment — e.g. section rules/divider weight or subtle heading emphasis (single preset bundle, PDF + Word parity)
- header density — e.g. “compact header” vs “airy” (name/tagline/contact spacing as a preset bundle)
- date presentation — preset: month+year vs season+year vs full date where applicable (data + template contract)
- contact line separator — preset: pipe · bullet · em dash (same string rules in HTML and Word)
- optional single accent color — if we can map cleanly to one token family both engines support (else skip)
- section title style — preset: small caps / sentence case / underline variant (layout-stable; template declares support)
- bullet / list glyph style — preset: disc vs dash vs square (CSS + Word list parity where possible)
- alignment where theme allows — e.g. header block center vs left while keeping section structure (only if layoutLocked allows)
- paper / export — Letter vs A4 as a first-class preset when we add non-US defaults
- hyphenation / rag control — on/off or “balanced rag” for narrow columns (preview + PDF; Word may differ slightly)
- “quiet” vs “expressive” bundles — named profiles that only remap approved token groups (guardrails for designers)

idea stack :
- parsing updated 
    - banks for schools
    - banks for types of major
    - types of degree
    - gpa stles
    - minor types
    - location api 
    - bank of domains (.com, .dev, .org, parsing as sites associated with whatever section)

- date selection (allowing before 1980)
- making site more relevant to non-engineers
    - no more tech stack revolving
    - allowing addition of links like github not defaulting
    - non-black and white papers (more styling options)

- project parser based on spacing and styling rather than as much of key words.

- idek if we have a parser for professional summary. 

- hard stuck button view when going backwards based on height (maybe buttons on side in same spot and set up rotates around that)

- logo creation.

- dark mode.

- ai clean up of parsed data potentially (example with the Skills & extracurriculars messy for one of my resumes)

- option to clear professional resume entry (just like a clear and are you sure button)

- skills, select a category, adn when  you add skills it adds to that space.

- better more active You're all set! screen.

- the ⓘ icon to specific parts of the site to help users understand their capabilities and what they might want to do.

- word doc / pdf preview :
    - more spacing between education entries. right now i have highlights basically resting on top of the next education entry. give it some proper spacing. 
    - 

- move the unsaved changes into gap between text entry and the zoom buttons. so its not in way of word or pdf icons.

- option to remove https:// from links.

- auto filled in fields like for linkedin, and github. (like https://github.com/ ... )

- drag and drop slides under too. (extra ui addition).

- google sso login. linkedin login. 

- solidified word - pdf and pdf - word comparisons for styling so as we create new templates we can maintain that.
    - not exact right now, need to be cleaned up a bit.

- hovering over sections in the pdf and going to it in the field.


- ai features
    - general resume clean up -
        - we pass all the information to the AI, all of the data available.
        - list how the data is set up based on the current template (like how things are sectioned)
        - list the goal (this will be different based on the ai function called)
        - set up so that the output of the AI is a shorter resume, or less bs words, or more organized sections.

    - clear up the assist output, right now extemely long.
    - allow ai to reorganize and set up honors & awards, clubs & extracurriculars, relevant coursework, etc.
    - verify all of our data is "touched" by the AI to verify if it should be tailored or not.
    - speed up tailoring process (however we can.)

- keep pipeline.py under ~400 lines.

- one page resume only templates. 


- ai preview - 

    - are you sure you want to navigate away while taylor.io is working ? 

- in our frontend / backend :
    - allow users to customize , resume 1 page or else ... [2, 3, 4]
    - choose how specific they want the tailoring to be, like trim all info, or be a bit more general
    - 

- with strict truth 
    - if not strict truth, summary and angle can be a bit more like "un-true", like targeting.

- with ai, make company mandatory input
    - weaken company name input

- ai generated tagline from user's initial resume in account set up.
- allowing adjust of ai speed, diff models potentially for faster model outputs
- same sort of thing except adjusting ai depth of analysis of job description and role
- active ui for ai, telling user whats occuring in pipeline process while the ai tailoring is progressing
- loading state for dashboard
- admin console for monitoring data collection.
- what are the default settings, do we want users to be able to adjust them, how do we handle it based on different backgrounds such as a engineer probably only really wants one page, a business person might want two to three to show off experiences and potential volunteer work.
- how do we want to aggregate the data for processing of JD's and user profiles.
- template images need to be cleared up, we want a clear picture of what the user is going to use.
- "tell us your goals", for job path, allowing user to set up adn target specific flows of the site.
- ai parsing of first parsed info for double checking / categorizing how to set up info.


- breakdown video of what hte site does after initial publishing

==== STYLING ====

Rounded shapes everywhere (no sharp corners)
Flat colors, minimal shading
Soft depth (very light shadows or layering)
Limited palette
brand pink (primary)
white / cream
2–3 soft accent colors (purple, teal, light blue)
Abstracted UI, not real UI
Human presence optional, but simplified

Stripe + Linear + Notion illustrations, but warmer