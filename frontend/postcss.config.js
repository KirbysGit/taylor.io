// postcss.config.js

// tells postcss what to do with our css.
// autoprefixer just adds the prefixes for older browsers.

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}

